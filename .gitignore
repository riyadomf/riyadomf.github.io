# Ignore the contents of the _site directory and other cache directories
_site/
.sass-cache/

# Ignore the directory for local files during development
local/

# Ignore the Gemfile that is generated
Gemfile.lock

# Ingore files created by npm
node_modules
package-lock.json

vendor/
.bundle

# Career knowledge base: lives in a separate PRIVATE repo cloned at docs/kb/.
# Never commit it here. See the "Knowledge base" section of .claude/CLAUDE.md.
docs/